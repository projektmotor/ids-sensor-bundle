# Nicht mit dem Paket ausliefern. Was hier fehlt, landet im Dist-Archiv und damit
# bei jedem Konsumenten — geprüft wird das mit `git archive HEAD | tar -t`.
#
# src/, config/, README.md, CHANGELOG.md und LICENSE bleiben drin: das erste Paar
# ist das Paket, das zweite erklärt es. CLAUDE.md dagegen richtet sich an die
# Entwicklung dieses Bundles und hat beim Konsumenten nichts zu suchen.
#
# /doc bleibt ebenfalls drin, seit es hier ausgeschlossen war: Die README verweist
# elfmal dorthin, und über Composer installiert waren das elf tote Verweise —
# ausgerechnet auf „Betrieb" und „Konfiguration", die ein Betreiber beim Deploy
# braucht. 232 KB Markdown sind der günstigere Preis. Durchgesetzt von
# `DocumentationTest::testNoLinkedDirectoryIsExcludedFromTheDistArchive()`.
/tests               export-ignore
/docker              export-ignore
/.github             export-ignore
/docker-compose.yml  export-ignore
/phpunit.xml.dist    export-ignore
/phpstan.neon.dist   export-ignore
/.php-cs-fixer.dist.php export-ignore
/Makefile            export-ignore
/CLAUDE.md           export-ignore
/.gitattributes      export-ignore
/.gitignore          export-ignore
